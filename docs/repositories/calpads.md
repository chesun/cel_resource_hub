# CALPADS — `calpads`

A short CALPADS data project — about a month of work, essentially a single Stata do file.

| | |
|---|---|
| **GitHub** | _Not yet created — a repo is planned_ |
| **Runs on (Scribe)** | _TODO: confirm if/where it runs_ |
| **Entry point** | A single do file (one-file project) |
| **Status** | Planned — repo to be created |

## What it is

A lightweight, short-duration analysis (roughly one month of work) built around a single do
file. The repository has not been created yet; once it exists, this page will link to it and
get the same header fields (GitHub URL, Scribe path, status) as the other repos.

!!! note "Context"
    CALPADS is the state's K–12 student-level administrative data system (the California
    Longitudinal Pupil Achievement Data System). This project worked with that data; the
    specifics will be filled in when the repo is created.

## Who to ask

- **Christina Sun** — author (`christinasun101@gmail.com` / `ucsun@ucdavis.edu`).

!!! todo "Next step"
    When the repo is created, update: GitHub URL, visibility (public/private), entry-point
    file name, and the Scribe location, and remove the placeholders above.
