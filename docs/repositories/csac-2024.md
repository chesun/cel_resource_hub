# CSAC 2024 Survey — `csac_2024`

Analysis code for the **2024** CSAC / California Education Lab high school senior survey work.

| | |
|---|---|
| **GitHub** | <https://github.com/chesun/csac_2024> (**private**) |
| **Runs on (Scribe)** | _TODO: confirm Scribe path_ |
| **Entry point** | `do/main.do` (Stata) |
| **Status** | Active; **no README in the repo** (confirmed — none locally or on GitHub) |
| **Start with** | This page + the folder map below |

## What it is

The 2024 analog of the CSAC high school senior-survey work, organized as a standard Stata
pipeline (`do/main.do` + `settings.do`, with `clean/`, `sample/`, `share/` stages) and an
`appendix` document.

!!! warning "No README — recommended to add one"
    Checked 2026-06-21: this repo has **no README**, in the working tree or on GitHub. For an
    inheriting researcher, a short `README.md` (what the project is, partner org, and how to
    run `do/main.do`) would make it self-explanatory. _Confirm partner organization(s) when
    adding it._

## Folder map (derived from the repo)

```
csac_2024/
├── do/
│   ├── main.do          # entry point
│   ├── settings.do      # path definitions
│   ├── macros_csac.doh  # shared macros
│   ├── clean/           # data cleaning
│   ├── sample/          # sample construction
│   ├── share/           # table/figure helpers
│   └── explore/         # ad-hoc / exploratory
├── tab/                 # tables output
├── log/                 # Stata logs
└── appendix.pdf / .doc  # appendix document
```

## Who to ask

- **Christina Sun** — author (`christinasun101@gmail.com` / `ucsun@ucdavis.edu`).

!!! todo "Recommended next step"
    Give this repo a short `README.md` (or `HANDOFF.md`) modeled on
    [`va_consolidated`](va-consolidated.md) so it can be inherited without tribal knowledge.
