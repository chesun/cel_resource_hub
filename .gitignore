# MkDocs build output
site/

# Python virtual environment
.venv/
venv/
__pycache__/
*.pyc

# macOS
.DS_Store

# Local recording aids for the VSCode-Stata demo (scratch; not site content)
recording-aids/
# Raw screen recordings (the optimized clip lives under docs/.../img/)
Screen Recording*.mov

# Hook runtime state (machine-local logs)
.claude/state/*.log
.claude/state/*.log.old
.claude/state/*.json
